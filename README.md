# cudasim

A Task graph based simulator for performance analysis of cuda codes

To Compile the lex analyzer, open terminal and pass the ommands:
lex appconfigscanner.l
gcc appconfigscanner.c lex.yy.c -o scanner